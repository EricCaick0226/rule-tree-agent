# Input Documents

Put real local PDF, Markdown, or text documents in this folder when testing the agent.

Files in this folder are ignored by git by default to avoid committing sensitive source documents.
