# Input Documents

Put real local Markdown or text documents in this folder when testing the default row-first agent.

PDF/OCR parser code remains in the repository as legacy/non-default infrastructure, but the default
row-first `agent_demo` / `run_agent` path rejects PDF input.

Files in this folder are ignored by git by default to avoid committing sensitive source documents.
